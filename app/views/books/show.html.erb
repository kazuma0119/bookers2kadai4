<!DOCTYPE html>
<html lang="ja">

 <head>
    <title>bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
<!下の部分は消しちゃダメ！>
    <script src="https://kit.fontawesome.com/f0e983ff20.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
 </head>

<main>
  <% flash.each do |message_type, message| %>
  <%= content_tag :div, message, class: message_type %>
<% end %>
   <p id="notice"></p>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <%= render 'users/info', book: @newbook %>
  <div class="col-md-8 offset-md-1">
    <h2>Book detail</h2>
    <table class="table table-hover table-inverse">
      <tbody>
         <tr>
          <td><%= image_tag @book.user.get_profile_image(100,100) %><br>
          <%= link_to user_path(@book.user_id), class:"nav-link text-blue" do %>
          <%= @book.user.name %></td>
          <% end %>
          <td><%= link_to book_path(@book.id), class:"nav-link text-blue" do %>
          <%= @book.title %></td>
          <% end %>
          <td> <%= @book.opinion %></td>
          <div class="link-item">
          <td><% if @book.user == current_user %>
          <%= link_to edit_book_path(@book.id), class:"nav-link btn btn-success" do %>
          Edit
          <% end %>
          <% end %>
          </td>
          <td><% if @book.user == current_user %>
          <%= link_to "Destroy", book_path(@book.id), method: :delete,class:"btn btn-danger", "data-confirm" => "本当に消しますか？" %>
          <% end %></td>
          <th colspan="3"></th>
        </tr>
      </tbody>
    </table>
  </div>
  </div>
  </div>
</main>