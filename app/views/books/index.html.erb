<!DOCTYPE html>
<html lang="ja">

 <head>
    <title>bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
<!下の部分は消しちゃダメ！>
    <script src="https://kit.fontawesome.com/f0e983ff20.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
 </head>

<main>
  <% if @book.errors.any? %>
  <%= @book.errors.count %>件のエラーが発生しました
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
  <p id="notice"></p>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">
        <h2>User info</h2>
          <%= image_tag current_user.get_profile_image(100,100) %>
        <table class="table">
          <tbody>
            <tr></tr>
            <tr>
              <th>name</th>
              <th><%= current_user.name %></th>
            </tr>
            <tr>
              <th>introduction</th>
              <th><%= current_user.introduction %></th>
            </tr>
          </tbody>
        </table>
        <div class="border padding-10px" style="height:38px;">
          <%= link_to edit_user_path(current_user.id), class:"text-grey d-flex justify-content-center align-items-center" do %>
          <i class="fa-solid fa-user-gear"></i>
          <% end %>
          </div>
        <h2 class="mt-3">New book</h2>
        <div class="form-group">
        <%= form_with model: @book do |f| %>
          <div class="field">
           <label for="book_title">Title</label><br>
        <%= f.text_field :title %>
          </div>

          <div class="field">
           <label for="book_body">opinion</label><br>
        <%= f.text_area :opinion %>
          </div>

        <%= f.submit 'Create Book', class:"btn btn-success" %>
        <% end %>
      </div>
    </div>
  <div class="col-md-8 offset-md-1">
    <h2>Books</h2>
    <table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @books.each do |book| %>
        <tr>
          <td><%= link_to user_path(book.id), class:"nav-link text-dark" do %>
          <%= image_tag book.user.get_profile_image(100,100) %></td>
          <% end %>
          <td><%= link_to book_path(book.id), class:"nav-link text-dark" do %>
          <%= book.title %></td>
          <% end %>
          <td><%= book.opinion %></td>
          <td colspan="3"></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  </div>
  </div>
</main>